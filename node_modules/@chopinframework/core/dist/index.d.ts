declare class Oracle {
    protected readonly callbackUrl: string;
    constructor(callbackUrl: string);
    notarize<T>(getter: () => T): Promise<T>;
    fetch: typeof fetch;
    now(): Promise<number>;
    random(): Promise<number>;
}

export { Oracle };
